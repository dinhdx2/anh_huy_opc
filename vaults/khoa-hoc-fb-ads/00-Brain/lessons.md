# Bài học khép-vòng (lessons.md)

> **Append-only**, 1 dòng/vòng đơn-vòng (luồng generic v3 PHA 3, `lib/loop.js`). Ghi khi `/vn-loop` chạy vá đơn-vòng.
> Song-vòng (leo tầng) ghi ở file tầng tương-ứng + `decisions-log.md` nhãn `altitude`. Khởi tạo 2026-07-03 (onboard).

<!-- format: [YYYY-MM-DD] <task/curve> — vòng #k: <đổi gì> → <kết-quả số thật> → <giữ/bỏ> -->

(chưa có — sẽ ghi khi khép-vòng task đầu có số thật qua /vn-loop)
